package pillar.java.exersice;
///This interface, which does not have an instantiated method, is an example of abstraction
//where this interface is acting as template for other classes focusing on the functionality 
//rather than the implementation 
public interface EarthLife {
public int reproduce();
}
